<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="estate_property_action" model="ir.actions.act_window">
            <field name="name">Propiedades</field>
            <field name="res_model">estate.property</field>
            <field name="view_mode">list,form</field>
        </record>

        <record id="view_estate_property_search" model="ir.ui.view">
            <field name="name">estate.property.search</field>
            <field name="model">estate.property</field>
            <field name="arch" type='xml'>
                <search string="Buscar Propiedades">
                    <field name="name"/>
                    <field name="postcode"/>
                    <field name="expected_price"/>
                    <field name="bedrooms"/>
                    <field name="living_area"/>
                    <field name="facades"/>

                    <filter string="Mis Propiedades" name="my_properties" domain="[('create_uid', '=', uid)]"/>

                    <group expand="0" string="Agrupar por">
                        <filter string="Usuario" name="group_by_user" context="{'group_by': 'create_uid'}"/>
                        <filter string="Fecha" name="group_by_date" context="{'group_by': 'create_date'}"/>
                        <filter string="Codigo Postal" name="group_by_postcode" context="{'group_by': 'postcode'}"/>
                    </group>    
                </search>
            </field>
        </record>
    </data>
</odoo>
